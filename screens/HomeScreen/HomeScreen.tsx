import { View, Text, Button } from 'react-native';

export const HomeScreen = ({ navigation }: any) => {
	return (
		<View>
			<Text>Home Screen</Text>
		</View>
	);
};

{
	/* <Button
				title="go to feed"
				onPress={() => navigation.navigate('feed', { name: 'melike' })}
			/> */
}
